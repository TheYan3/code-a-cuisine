import { Component } from '@angular/core';

/**
 * Generator page. Will let the user enter ingredients they have on hand and
 * trigger an n8n workflow that generates a matching recipe via AI.
 */
@Component({
  imports: [],
  selector: 'app-generator',
  styleUrl: './generator.scss',
  templateUrl: './generator.html',
})
export class Generator {}
